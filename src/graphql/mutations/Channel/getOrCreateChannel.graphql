mutation GetOrCreateChannel($input: GetOrCreateChannelInput!) {
  getOrCreateChannel(input: $input) {
   errors {
      field
      message
    }
    channel {
      id
      name
    } 
     }
}
