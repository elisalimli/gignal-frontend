mutation CreateChannel($input: CreateChannelInput!) {
  createChannel(input: $input) {
    errors {
      field
      message
    }
    channel {
      id
      name
      public
      teamId
    }
  }
}
