import styled from "styled-components";
import { theme } from "../theme";

export const IndexTeamsWrapper = styled.div`
  display: flex;
  justify-content: center;
  align-items: center;
  margin: 0 auto;
  padding: 0.75rem;
  max-width: ${theme.widths["2xl"]};
`;
