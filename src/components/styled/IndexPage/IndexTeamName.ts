import styled from "styled-components";
import { theme } from "../theme";

export const IndexTeamName = styled.h2`
  font-weight: ${theme.fontWeight.semibold};
  font-size: ${theme.fontSize["2xl"]};
`;
