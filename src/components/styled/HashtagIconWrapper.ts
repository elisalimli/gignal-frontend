import theme from "@chakra-ui/theme";
import styled from "styled-components";

export const HashtagIconWrapper = styled.svg`
  color: ${theme.colors.black};
  width: 1.25rem;
  height: 1.25rem;
`;
